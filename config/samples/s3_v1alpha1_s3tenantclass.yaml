apiVersion: s3.bedag.ch/v1alpha1
kind: S3TenantClass
metadata:
  labels:
    app.kubernetes.io/name: storagegrid-operator
    app.kubernetes.io/managed-by: kustomize
  name: s3tenantclass-sample
spec:
  # TODO(user): Add fields here
