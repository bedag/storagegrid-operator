---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: storagegrids.s3.bedag.ch
spec:
  group: s3.bedag.ch
  names:
    kind: StorageGrid
    listKind: StorageGridList
    plural: storagegrids
    shortNames:
    - sg
    - sgs
    singular: storagegrid
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The current readiness state of the StorageGrid
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Deletion policy by default for tenants
      jsonPath: .status.defaultTenantDeletionPolicy.policy
      name: DeletionPolicy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageGrid is the Schema for the storagegrids API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageGridSpec defines the desired state of StorageGrid.
            properties:
              defaultBucketRegion:
                description: Default region for buckets created in the StorageGrid,
                  defaults to the first region in the list.
                type: string
              defaultTenantDeletionPolicy:
                default:
                  retentionDuration: 168h
                description: |-
                  DefaultDeletionPolicy specifies how associated resources (e.g., Tenants) should be handled.
                  when the Tenant resource is deleted.
                  A tenant will always be able to override this policy if specified in the Tenant resource.
                  By default this is set to Retain with a retention duration of 7 days.
                  The maximum retention duration is 30 days.
                properties:
                  policy:
                    default: Retain
                    description: |-
                      Policy specifies the deletion policy for tenants.
                      Possible values:
                      - "Delete": Tenants are deleted on the backend immediately.
                      - "Retain": (default) Tenants are retained and need to be manually deleted later.
                      - "RetainThenDelete": Tenants are retained for the duration specified in RetentionDuration, then deleted.
                    enum:
                    - Delete
                    - Retain
                    - RetainThenDelete
                    type: string
                  retentionDuration:
                    default: 168h
                    description: |-
                      RetentionDuration specifies the duration to retain resources after deletion is requested.
                      Accepts values like "60m", "24h", etc., using the standard Kubernetes duration format.
                      set this to 0 to disable retention.
                      Defaults to "168h" (7 days).
                    type: string
                    x-kubernetes-validations:
                    - message: retentionDuration must not exceed 30 days
                      rule: duration(self) <= duration('720h')
                type: object
              managementEndpoint:
                description: |-
                  ManagementEndpoint is the StorageGrid admin API endpoint for tenant management,
                  user creation, quota configuration, and administrative operations.
                  Example: https://grid-admin.internal.example.com
                type: string
              maxUnavailableNodes:
                default: 1
                description: |-
                  Amount of nodes (default: 1) that can be unavailable before the StorageGrid is considered not ready.
                  This is a safeguard to prevent operations when the StorageGrid is not fully available.
                type: integer
              operations:
                description: |-
                  Operations configures execution parameters for long-running StorageGrid operations.
                  These settings tune how operations execute, not what operations to perform.
                properties:
                  drain:
                    description: |-
                      Drain configures bucket drain operation execution parameters.
                      Used by all bucket drain operations unless overridden at bucket level.
                    properties:
                      initialPollInterval:
                        default: 3m
                        description: |-
                          InitialPollInterval defines how often to check drain progress during.
                          the first hour of operation (when StorageGrid typically makes faster progress).
                        type: string
                      longRunningPollInterval:
                        default: 30m
                        description: |-
                          LongRunningPollInterval defines how often to check drain progress after
                          the first hour (when operations typically slow down for large buckets).
                        type: string
                      stuckThreshold:
                        default: 3h
                        description: |-
                          StuckThreshold defines how long without progress before considering a drain stuck.
                          If object count doesn't decrease within this duration, a warning event is emitted.
                        type: string
                    type: object
                type: object
              s3OperationsTenantClass:
                description: |-
                  S3OperationsTenantClass optionally specifies which S3TenantClass the operator uses.
                  for S3 API operations (eg. bucket policies).
                  If not set, S3 operations use each bucket's tenant-specific S3TenantClass endpoint.
                  Use this when operator pods cannot reach all tenant-specific endpoints but can.
                  access a specific gateway endpoint (e.g., internal management network).
                type: string
              secretRef:
                description: The secret containing the credentials for the StorageGrid.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              tenantPrefix:
                default: Disabled
                description: |-
                  Controls whether tenant names should be prefixed in the backend.
                  Possible values:
                  - "Disabled": (default) No prefixing.
                  - "Namespace": Namespace is added as a prefix (e.g., "namespace-tenant").
                enum:
                - Disabled
                - Namespace
                type: string
            required:
            - secretRef
            type: object
          status:
            description: StorageGridStatus defines the observed state of StorageGrid.
            properties:
              conditions:
                description: |-
                  Track StorageGrid conditions.
                  Track s3Tenant conditions.
                  Conditions is an array of conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              defaultBucketRegion:
                description: DefaultRegion for buckets created in the StorageGrid.
                type: string
              defaultTenantDeletionPolicy:
                default: {}
                description: Currently active tenant deletion policy.
                properties:
                  policy:
                    default: Retain
                    description: |-
                      Policy specifies the deletion policy for tenants.
                      Possible values:
                      - "Delete": Tenants are deleted on the backend immediately.
                      - "Retain": (default) Tenants are retained and need to be manually deleted later.
                      - "RetainThenDelete": Tenants are retained for the duration specified in RetentionDuration, then deleted.
                    enum:
                    - Delete
                    - Retain
                    - RetainThenDelete
                    type: string
                  retentionDuration:
                    default: 168h
                    description: |-
                      RetentionDuration specifies the duration to retain resources after deletion is requested.
                      Accepts values like "60m", "24h", etc., using the standard Kubernetes duration format.
                      set this to 0 to disable retention.
                      Defaults to "168h" (7 days).
                    type: string
                    x-kubernetes-validations:
                    - message: retentionDuration must not exceed 30 days
                      rule: duration(self) <= duration('720h')
                type: object
              ready:
                default: false
                description: Displays whether the StorageGrid is ready or not.
                type: boolean
              regions:
                description: list of available regions for tenants.
                items:
                  type: string
                type: array
              usage:
                description: Usage of the StorageGrid summarizes the usage of all
                  tenants.
                properties:
                  tenantCount:
                    description: total tenants linked to the StorageGrid.
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
